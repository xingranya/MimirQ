# SeeWayK 知识库设计系统

## Direction

Operate-first 的企业知识工作台。视觉使用中性浅灰/白色页面、清晰的蓝色主操作和细边界，优先保证扫描、比较、输入和状态判断。层级来自留白、字号、字重和位置，不依赖渐变、玻璃、光晕或大阴影。

## Visual Rules

- 圆角只使用 6px 或 8px；图标按钮使用固定方形尺寸。
- 间距使用 4、8、12、16、24、32 的刻度。
- 字号使用 12、13、14、16、20、24；正文不低于 14px。
- 主色只承担选中、主要操作和焦点；成功、警告、错误使用语义色。
- 页面一级操作每个视图只保留一个，次要操作进入菜单或抽屉。
- 页面结构使用线性布局和分隔线，不嵌套卡片。

## Interaction Rules

- 侧栏保持挂载，导航只更新主内容和 URL。
- 当前导航项只有一个指示条；侧栏分组和滚动位置跨路由保留。
- 对话高级能力进入统一设置抽屉；引用、诊断和思考步骤默认折叠。
- 设置按任务分组，搜索和高级折叠优先于长页展示。
- 所有加载、错误、禁用和保存状态都必须有明确的文字或语义反馈。

## Responsive Rules

- 桌面侧栏展开 224px、折叠 56px；移动端使用覆盖层。
- 主内容滚动边界稳定，禁止页面壳层和内部滚动同时抢夺滚轮。
- 目标视口为 1440x900、1280x800、1024x768、768x1024、390x844。
