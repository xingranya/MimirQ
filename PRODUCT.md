# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

知识库运营人员、企业内部知识使用者和拥有租户管理权限的管理员。运营人员负责上传、解析、治理和验证文档；使用者主要通过对话检索知识；管理员负责模型、权限和运行状态配置。

## Product Purpose

见外传媒知识库把文档解析、切块、索引、检索、引用和评测组织成可检查的企业知识工作流。成功标准是用户能快速找到可验证的证据，运营人员能定位知识质量问题，管理员能清楚完成配置而不被低频选项淹没。

## Positioning

产品的核心机制是把检索证据、处理阶段和质量回归显式呈现，而不是只返回一个不可解释的答案。内部 API、数据结构和可观测性链路是系统约束，用户界面可以重组但不能隐藏必要的证据和状态。

## Operating Context

用户在桌面浏览器和窄屏设备上使用单页式应用，主要工作流包括首页问答、数据集与文档管理、入库解析、检索验证、图谱分析和系统设置。对话支持 SSE 流式事件，设置可能包含租户级和本地浏览器级配置。

## Capabilities and Constraints

- 现有路由、权限、API、SSE 事件和数据结构必须保持兼容。
- 品牌按场景分三级：紧凑位置使用“SEEWAY”，标准品牌区使用“SEEWAY 见外”，正式标题使用“见外传媒知识库”；助手称为“我是见外传媒知识库”，用户可见字母标志使用 S。
- 前端采用 Next.js App Router、React、Tailwind、Radix 和现有组件库。
- 功能不删除，低频能力通过抽屉、折叠或更多菜单渐进呈现。
- 默认中文界面；内部 `mimirq` API 路径、数据库字段、缓存键、事件名和部署名保留。

## Brand Commitments

见外传媒知识库是本轮用户指定的新用户可见品牌。Logo 使用用户提供的 `seeway 见外` 图片，并为紧凑场景保留不裁字的纯 `SEEWAY` 版本。视觉方向是简约、扁平、低装饰、单一蓝色强调和中性浅色背景；不使用渐变、毛玻璃或装饰性发光作为主要层级手段。

## Evidence on Hand

- 现有源码位于 `web/`，对话、侧栏、设置和 AppFrame 均已有实现。
- `.zread/wiki/` 包含项目结构、流式对话和知识工作台的代码知识库。
- 实机地址为 `http://100.78.64.62:3000`，已通过 ego-browser 查看首页、侧栏、知识库和设置页面。
- 当前没有批准用于产品宣传的额外图片、客户案例或性能声明，界面不应编造此类内容。

## Product Principles

1. 先完成用户的当前任务，再暴露高级能力。
2. 证据和运行状态必须可追溯，但默认不打断主流程。
3. 同一动作在不同页面使用一致的控件、反馈和键盘行为。
4. 路由切换保持应用壳层和用户现场，避免不必要的完整刷新感。

## Accessibility & Inclusion

保留现有键盘导航、跳过链接、焦点管理和移动端侧栏隔离；所有图标按钮提供可读标签或 tooltip；窄屏视口不能产生重叠、裁切或不可点击控件。
